# Sofra 🍽️ — نظام مطعم متكامل (User + Admin)

## ⚡ التشغيل السريع (3 خطوات بس)

المشروع محتاج **PHP + MySQL** عشان الـ Login/Register يشتغلوا فعليًا (لأن فيه قاعدة بيانات حقيقية وراه، مش شكل بس). أسهل طريقة: **XAMPP**.

### 1) حمّل XAMPP وشغّله
- حمّل XAMPP من https://www.apachefriends.org (فيه Apache + PHP + MySQL جاهزين).
- افتح XAMPP Control Panel وشغّل **Apache** و **MySQL**.

### 2) حط المشروع في مكانه
- انسخ مجلد المشروع بالكامل (بكل اللي جواه) جوه:
  ```
  C:\xampp\htdocs\sofra
  ```

### 3) جهّز قاعدة البيانات
- افتح المتصفح على: `http://localhost/phpmyadmin`
- اضغط **Import** واختار الملف: `backend/config/schema.sql`
- كده الداتا بيز `sofra_db` هتتعمل تلقائيًا ومعاها حساب أدمن جاهز:
  - **Email:** admin@sofra.com
  - **Password:** admin123

### 4) افتح المشروع
افتح المتصفح على:
```
http://localhost/sofra/
```
هتتحول أوتوماتيك لصفحة **اختر نوع الحساب (User / Admin)** — بالظبط زي ما طلبت. 🎉

---

## 🧭 رحلة الاستخدام

1. **صفحة الاختيار** (`choice.html`) → تختار **مستخدم** أو **مدير**.
2. **صفحة تسجيل الدخول / تسجيل حساب جديد** → تظهر علامة توضح نوع الحساب اللي اخترته.
3. لو بياناتك صح:
   - **مستخدم** → يدخل على واجهة **Sofra_Fronten** (الموقع بتاع الزباين: المنيو، الكارت، الطلبات...).
   - **مدير** → يدخل على **لوحة تحكم أدمن** كاملة (Dashboard، الطلبات، المنيو، العملاء، التقارير...) مبنية على ثيم ArchitectUI واتخصصت باسم سفرة.
4. زرار الخروج (Logout) في أي مكان بيمسح الجلسة صح ويرجعك لصفحة الاختيار.

---

## 📁 هيكل المشروع (بعد التنظيف)

```
sofra/
├── index.html                      ← أول ما تفتح المشروع يوديك على صفحة الاختيار
│
├── Sofra_Fronten/                  ← واجهة المستخدم (Frontend)
│   ├── pages/                      (choice, login, index, menu, cart, about, contact...)
│   ├── partials/                   (navbar + footer مشتركين لكل الصفحات)
│   └── assets/                     (css, js, images)
│
├── architectui-html-theme-free/    ← لوحة تحكم الأدمن (Admin Dashboard)
│   └── architectui-html-free/      ← ✅ النسخة الجاهزة للتشغيل مباشرة (مبنية مسبقًا)
│       (index, orders, meals-all, meals-add, meals-categories,
│        customers, reviews, reports, settings)
│   └── src/                        (الكود المصدري - للتعديل المستقبلي فقط، مش لازم للتشغيل)
│
└── backend/                        ← الـ API بتاع الـ PHP
    ├── api/        (login.php, register.php, validate.php, logout.php)
    ├── auth/       (Auth.php - كل منطق تسجيل الدخول والتشفير)
    ├── config/     (database.php, schema.sql)
    └── middleware/ (AuthMiddleware.php)
```

**ملحوظة:** مسحنا كل الملفات اللي مالهاش لازمة (ملف فاضي، ملف Logout قديم مكرر، ومجلد node_modules اللي كان 213 ميجا ومش لازم للتشغيل بعد ما عملنا build للوحة الأدمن).

---

## 🔐 لو عايز تعدّل بيانات الداتا بيز

في `backend/config/database.php`:
```php
private $host = "localhost";
private $db_name = "sofra_db";
private $username = "root";   // اسم مستخدم MySQL بتاعك
private $password = "";       // باسورد MySQL بتاعك (فاضي في XAMPP الافتراضي)
```

---

## 🛠️ لو عايز تعدّل تصميم لوحة الأدمن مستقبلًا

الكود المصدري موجود في `architectui-html-theme-free/src`. عشان تبني نسخة جديدة بعد أي تعديل:
```bash
cd architectui-html-theme-free
npm install
npm run build
```
هيولّد نسخة محدثة في `architectui-html-free/`.
(الخطوة دي **مش لازمة** لو مش هتعدل حاجة — النسخة الجاهزة شغالة كده كده.)

---

## ✅ الإصلاحات اللي اتعملت في المراجعة دي
- بنينا لوحة الأدمن (production build) عشان تشتغل فورًا من غير Node.js أو تعقيد.
- صلحنا مسار الـ Logout في لوحة الأدمن (كان بيوديك لمكان غلط).
- صلحنا زرار Logout في هيدر الأدمن (كان مربوط بملف PHP قديم مكسور).
- صلحنا مسارات absolute path غلط في 4 صفحات (`about`, `contact`, `cart`, `index`) كانت هتكسر الصور والـ CSS حسب إعدادات السيرفر.
- ضفنا badge بسيط في صفحة تسجيل الدخول يوضح نوع الحساب المختار.
- مسحنا الملفات الزيادة (ملف فاضي، كود مكرر، node_modules).
